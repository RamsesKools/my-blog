services:
  nginx:
    image: nginx:alpine
    container_name: blog
    restart: unless-stopped
    ports:
      - "20001:80"
    volumes:
      - ./site:/usr/share/nginx/html:ro
      - ./nginx.conf:/etc/nginx/conf.d/default.conf:ro
    networks:
      - traefik-proxy
    labels:
      - "traefik.enable=true"
      - "traefik.http.routers.blog-preview.rule=Host(`blog-preview.ramseskools.nl`)"
      - "traefik.http.routers.blog-preview.entrypoints=web"
      - "traefik.http.routers.blog-preview.middlewares=redirect-https@file"
      - "traefik.http.routers.blog-preview-secure.rule=Host(`blog-preview.ramseskools.nl`)"
      - "traefik.http.routers.blog-preview-secure.entrypoints=websecure"
      - "traefik.http.routers.blog-preview-secure.tls=true"
      - "traefik.http.routers.blog-preview-secure.tls.certresolver=letsencrypt"
      - "traefik.http.routers.blog-preview-secure.middlewares=custom-errors@file"
      - "traefik.http.services.blog-preview.loadbalancer.server.port=80"

networks:
  traefik-proxy:
    external: true
